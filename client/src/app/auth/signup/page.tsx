import { SignupView } from "@/auth/view";

// ----------------------------------------------------------------------

export const metadata = { title: "Sign Up" };

export default function Page() {
  return <SignupView />;
}
