import { SignInView } from "@/auth/view";

// ----------------------------------------------------------------------

export const metadata = { title: "Sign In" };

export default function Page() {
  return <SignInView />;
}
