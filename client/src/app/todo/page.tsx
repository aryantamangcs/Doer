import { TodoView } from "@/modules/todo/view";

// ----------------------------------------------------------------------

export const metadata = { title: "Todo" };

export default function Page() {
  return <TodoView />;
}
